pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-web-proxy-app'

project(':omar-web-proxy-app').projectDir = "${rootProject.projectDir}/apps/omar-web-proxy-app" as File
